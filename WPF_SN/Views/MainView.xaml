<Window x:Class="WPF_SN.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_SN.Views"
        mc:Ignorable="d"
        Title="Messanger"  Height="{Binding Height, Mode=TwoWay}" Width="{Binding Width, Mode=TwoWay}" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" WindowStyle="None" >
    
    <Grid>
        <ContentControl Content="{Binding CurrentPageViewModel, Mode=TwoWay}" />
    </Grid>
</Window>
